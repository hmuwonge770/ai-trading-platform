# Stage AN — CI Verification

Phase 5 requires the repository lint and test workflow plus the Laravel dashboard test workflow to pass on the Stage AN head commit before merge. After merge, the resulting `main` commit must be checked independently before Stage AN is considered complete.
