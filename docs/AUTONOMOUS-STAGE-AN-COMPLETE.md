# Stage AN — Completion Criteria

Stage AN is complete only when its five implementation phases are complete, CI is green, the pull request is merged to `main`, and post-merge CI is verified. Until those checks pass, Stage AN remains in progress.
