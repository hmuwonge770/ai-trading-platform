Stage U adds a fail-closed live execution boundary after human authorization consumption and deterministic risk approval. It requires explicit LIVE control state, a permitted live promotion stage, matching strategy identity, and unique client order IDs before delegating to an injected submitter. It does not create an exchange client, own credentials, approve authorization, change capital, or bypass risk, kill-switch, circuit-breaker, reconciliation, accounting, or recovery controls.