# Stage AB delivery contract

`OperationalAlertSink.deliver(alert)` is the sole external capability exposed by the delivery boundary. Implementations must treat the alert as immutable and must not receive or access exchange credentials.
