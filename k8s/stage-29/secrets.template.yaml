# Template only. Never commit real secret values.
# Create these as cluster-managed Secrets using an external secret manager.
apiVersion: v1
kind: Secret
metadata:
  name: testnet-binance
  namespace: trading-testnet
type: Opaque
stringData:
  BINANCE_API_KEY: "REPLACE_ME"
  BINANCE_API_SECRET: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: live-binance
  namespace: trading-live
type: Opaque
stringData:
  BINANCE_API_KEY: "REPLACE_ME"
  BINANCE_API_SECRET: "REPLACE_ME"
